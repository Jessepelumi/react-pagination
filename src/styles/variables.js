import { css } from "styled-components";

const variables = css`
  :root {
    --font-custom: "SpaceGrotesk";

    --transparent: transparent;
    --white: #FFFFFF;
    --creame: #F0EBE3;
    --blue: #1679AB;
    --slate: #161A30;
  }
`;

export default variables;